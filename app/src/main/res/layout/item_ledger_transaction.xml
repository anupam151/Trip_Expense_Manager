<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://tools.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="#FFFFFF"
    android:paddingVertical="6dp"
    android:paddingHorizontal="8dp">

    <LinearLayout
        android:id="@+id/row_content"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingHorizontal="2dp"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <TextView
            android:id="@+id/txt_row_date"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            tools:text="02 Jul 2026"
            android:textColor="#333333"
            android:textSize="13sp" />

        <TextView
            android:id="@+id/txt_row_purpose"
            android:layout_width="0dp"
            android:layout_weight="1.5"
            android:layout_height="wrap_content"
            tools:text="Hotel Booking"
            android:textColor="#333333"
            android:textSize="13sp"
            android:paddingStart="6dp"
            android:paddingEnd="4dp"/>

        <TextView
            android:id="@+id/txt_row_debit"
            android:layout_width="0dp"
            android:layout_weight="1.2"
            android:layout_height="wrap_content"
            tools:text="1,200.00"
            android:gravity="center"
            android:textColor="#D32F2F"
            android:textStyle="bold"
            android:textSize="13sp" />

        <TextView
            android:id="@+id/txt_row_credit"
            android:layout_width="0dp"
            android:layout_weight="1.2"
            android:layout_height="wrap_content"
            tools:text="-"
            android:gravity="center"
            android:textColor="#388E3C"
            android:textStyle="bold"
            android:textSize="13sp" />

        <ImageButton
            android:id="@+id/btn_edit_transaction"
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:src="@drawable/edit_square"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:scaleType="fitCenter"
            android:padding="5dp"
            android:contentDescription="Edit" />

        <ImageButton
            android:id="@+id/btn_delete_transaction"
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:src="@drawable/ic_delete_trip"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:scaleType="fitCenter"
            android:padding="5dp"
            android:contentDescription="Delete" />

    </LinearLayout>

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_below="@id/row_content"
        android:layout_marginTop="6dp"
        android:background="#EAEAEA" />

</RelativeLayout>